const Fallback = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h2>Loading...</h2>
    </div>
  );
};

export default Fallback;
