const Fallback = () => {
  return (
    <div
      style={{
        position: "relative",
        top: 50,
        margin: "0 auto",
      }}
    >
      <h2>Loading...</h2>
    </div>
  );
};

export default Fallback;
